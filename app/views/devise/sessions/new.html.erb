<h2>Sign in</h2>

<h4>If you've registered with ReunionPong in the past your account should still be here.</h4>
You can use the "Forgot your password" link below to recover your password or
<%= link_to "email us", "mailto:reunionpong@gmail.com" %>
if you are having any troubles.
</br>
</br>


<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email, :required => false, :autofocus => true %>
    <%= f.input :password, :required => false %>
    <%= f.input :remember_me, :as => :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Sign in" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
